using UnityEngine;
using System.Collections;

//These can be set if needed runtime, othervice you set tags on the prefab inside unity
public enum UnitTypesEnum {
    Projectile,
    Player,
    Box,
    Candle,
    Portal,
    ProjectileShooter
}
